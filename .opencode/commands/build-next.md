# build-next — implement next TASKS.md item
1. Read TASKS.md, ENGINE.md, game-design.md
2. Implement smallest unchecked box + headless test
3. Run `python -m pytest -q` and `python -m game.main --smoke`
4. Commit with `feat/fix/test:` prefix. Push if CI green.
